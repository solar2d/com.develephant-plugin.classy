Sources for the plugin `plugin.classy`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.classy" = {
            publisherId = "com.develephant",
        },
    },
}
```
